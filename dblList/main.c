#include<stdio.h>
#include<stdlib.h>
typedef struct Node {
    int data;
    struct Node* next;
    struct Node* prev;
} Node;
typedef Node * DblList;
Node * getNode(int v){
} 
void printDblList(DblList head){
}
void insert(DblList head){
}
int main(){
    DblList head = NULL;
    insert(5);
    insert(7);
    printDblList(head);
}

